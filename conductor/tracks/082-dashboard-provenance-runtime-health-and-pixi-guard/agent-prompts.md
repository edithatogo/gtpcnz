# Agent Prompts

## Implementation Agent

Add chart provenance panels, runtime/deployment health metadata, and a Prefix.dev Pixi guard. Keep all metadata public-safe, avoid secrets, and preserve uv compatibility.

## Review Agent

Review for secret leakage, private path exposure, misleading health/model-validity language, missing calculation mode, missing caps/seeds, and Pixi guard false positives.
