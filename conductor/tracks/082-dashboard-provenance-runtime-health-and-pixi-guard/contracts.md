# Contracts

- Provenance panels must not expose secrets or local user paths.
- Every chart bundle must identify calculation mode and not-valid-for claims.
- Runtime health must not be used to imply model validation.
- Pixi guard must fail clearly when `pixi` is not Prefix.dev Pixi.
- Existing uv-compatible workflows must not be broken.
