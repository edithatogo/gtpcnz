# Track 074 - Aggregate validation claim-surface review

Review app, report, model-card, README, and release wording after the aggregate validation lane moved to `public_aggregate_validated` / `empirically_supported_if_gated`.

This track covers:

- updating public-facing language so aggregate validation is visible and accurate;
- preserving not-valid-for warnings for precise fiscal, ED, hospital-demand, workforce, implementation, and causal claims;
- checking Streamlit cockpit badges, downloads, model cards, README links, and Quarto report language;
- preventing stale `calibration_readiness_only` text from appearing as current status where aggregate validation now passes.

This track does not add evidence or broaden claims.
