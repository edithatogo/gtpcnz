# Acceptance

- A processed PHO Services Agreement table artefact exists with hash sidecar, or the track records a deterministic extraction blocker.
- Public processed schema checks cover the new artefact if created.
- Tests prove the transform is deterministic against the checked-in raw PDF.
- CAL-G-005 status is not weakened; reference-only evidence does not block or inflate the numeric comparison lane.
- Docs explain what the extracted tables are and are not valid for.
