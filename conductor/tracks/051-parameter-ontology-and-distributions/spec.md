# Parameter ontology and uncertainty distributions

Goal: Fully parameterise every public-model assumption, coefficient, bound, and mapping.

Claim boundary: preserve public benchmark status unless gates pass.
