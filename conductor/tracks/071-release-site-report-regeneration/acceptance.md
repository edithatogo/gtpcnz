# Acceptance

- `scripts/generate_release_model_card.py --check-only` passes.
- `scripts/generate_release_manifest.py --check-only` passes.
- Quarto render completes from a clean checkout or documents an external renderer/dependency blocker.
- Generated public release artefacts show aggregate validation status without precision or causal claim expansion.
- `docs/release/` records the regenerated artefact hashes and verification commands.
