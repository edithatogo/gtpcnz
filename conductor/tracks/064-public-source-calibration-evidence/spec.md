# Track 064: Public source acquisition and calibration evidence

## Goal

Convert the post-063 source-readiness frontier into an evidence ledger that reflects verified public source files, checksums, processed artefacts, and calibration gate outcomes without upgrading unsupported public claims.

## Scope

- Record that all six public source artefacts have verified checksums and processed outputs.
- Record that baseline public aggregate reproduction and posterior predictive checks pass.
- Keep calibration status at `calibration_readiness_only` because temporal, geographic, subgroup, and policy-shock validation datasets remain unavailable.
- Add regression coverage so documentation cannot drift back to `pending-download` / `0/6` source language.

## Non-Goals

- Do not claim linked-data calibration.
- Do not use private administrative, patient-level, confidential, stakeholder, or unpublished expert-elicitation inputs.
- Do not claim precise fiscal, ED, hospital-demand, workforce, implementation, or causal effects.
