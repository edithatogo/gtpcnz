# Acceptance

- Dash includes a model coverage matrix.
- Each public-safe engine has a route or visible section, table fallback, caveat, and download.
- Existing Streamlit-only standalone pages are either represented in Dash or explicitly marked deferred.
- `models/tests/test_dash_app.py` includes model-surface coverage assertions.
- Runtime, Dash, engine-adapter, public-only, and concern-boundary checks pass.
