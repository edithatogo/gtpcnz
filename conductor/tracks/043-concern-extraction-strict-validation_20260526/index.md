# Concern Extraction and Strict Validation Architecture

## Status

Complete.

## Purpose

Separate the model's public-data inputs, parameter definitions, scenario overrides, calculation engines, result schemas, UI controls, and audit/claim-boundary metadata into explicit typed contracts.

## Files

- `metadata.json` - track status and registry metadata.
- `spec.md` - problem statement, current coupling map, target architecture, invariants, and acceptance criteria.
- `plan.md` - implementation phases and validation gates.

## Parent registry

- `conductor/tracks.md` when present in the active checkout.
