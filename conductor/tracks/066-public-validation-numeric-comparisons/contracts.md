# Contracts

## CON-PUBLIC-001

The numeric extract is derived only from the public Health NZ access-to-primary-care workbook.

## CON-CAL-001

CAL-G-003 and CAL-G-004 may report `public_validation_numeric_ready`, but they must not report `passed` until model predictions are compared against held-out public aggregate values and the comparison passes.
