# Agent Prompts

Use this track only for Pixi runtime and deployment reproducibility.

Allowed moves:

- update Pixi tasks/environments and Dash deployment files;
- use `prefix-dev/setup-pixi` in GitHub Actions;
- regenerate `pixi.lock` only with Prefix.dev Pixi, never with the local Pixiv downloader.

Forbidden moves:

- do not delete uv compatibility before parity;
- do not use the local Pixiv downloader `pixi.exe` for dependency management;
- do not claim precise fiscal savings, ED reductions, hospital-demand reductions, workforce effects, implementation impacts, or causal effects.
