# Acceptance

- Public holdout comparison CLI runs in readiness mode.
- `--require-pass` fails while comparisons exceed tolerance.
- CAL-G-003 and CAL-G-004 report `public_holdout_comparison_failed`.
- Public aggregate calibration still returns `public_benchmark` / `calibration_readiness_only`.
