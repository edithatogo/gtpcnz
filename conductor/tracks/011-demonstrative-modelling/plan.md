# Plan

1. Encode a `Scenario` dataclass with normalised levers.
2. Implement 14 game-specific model functions.
3. Generate CSV outputs for all game-scenario combinations.
4. Generate charts summarising welfare and hospital pressure.
5. Generate per-game model cards and a modelling report.
6. Add tests for coverage, bounded outputs and core directional behaviour.
7. Add an audit matrix identifying calibration status and next validation step.
