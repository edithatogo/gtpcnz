# Contracts

- Shared URLs must be deterministic and reversible through tests.
- URL parameters must not exceed runtime caps.
- Custom scenarios must never be described as calibrated policy recommendations.
- Scenario exports must include claim boundary, seed, caps, app/model version, and input parameters.
- Guided mode must not hide public caveats or evidence gaps.
