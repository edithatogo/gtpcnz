# Agent Prompts

## Implementation Agent

Implement shareable Dash URLs, guided reader mode, and bounded custom scenario builder/export. Keep all custom scenario language educational/model-index only and add round-trip tests for URL state.

## Review Agent

Review for URL-state bugs, uncapped parameters, hidden caveats, scenario-builder overclaiming, and exports that omit provenance or claim-boundary metadata.
