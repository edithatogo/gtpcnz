# Public Parameter Ontology v1

Every public-model coefficient must resolve to `parameters.public.v1.yaml` with source, bounds, distribution, sensitivity priority, calibration role, and claim boundary.

Formula refs currently registered: `capitation_component`, `scheduled_activity_component`, `population_denominator`, `workforce_supply_component`, `access_component`.
